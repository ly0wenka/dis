from docx import Document
from docx.shared import Pt
from docx.enum.text import WD_ALIGN_PARAGRAPH
import math2docx

# ============================================================================
# СТВОРЕННЯ ДОКУМЕНТА
# ============================================================================

doc = Document()

# Налаштування стилю
style = doc.styles['Normal']
style.font.name = 'Times New Roman'
style.font.size = Pt(14)

# ============================================================================
# ЗАГОЛОВОК
# ============================================================================

title = doc.add_heading('Еволюційний бегінг для ансамблевого навчання', level=1)
title.alignment = WD_ALIGN_PARAGRAPH.CENTER

authors = doc.add_paragraph('Джанг Нго, Родні Берд, Рохіташ Чандра')
authors.alignment = WD_ALIGN_PARAGRAPH.CENTER
authors.runs[0].italic = True

aff = doc.add_paragraph('Університет Нового Південного Уельсу, Сідней, Австралія')
aff.alignment = WD_ALIGN_PARAGRAPH.CENTER

doc.add_paragraph()

# ============================================================================
# АНОТАЦІЯ
# ============================================================================

doc.add_heading('Анотація', level=2)

abstract_text = """Ансамблеве навчання досягло успіху в машинному навчанні з основними перевагами перед іншими методами навчання. Бегінг є провідним методом ансамблевого навчання, який створює підгрупи даних, відомі як "bags" (мішки), що навчаються індивідуальними методами машинного навчання, такими як дерева рішень. Випадковий ліс є видатним прикладом бегінгу з додатковими функціями в процесі навчання.

Еволюційні алгоритми були видатними для задач оптимізації, а також використовувалися для машинного навчання. Еволюційні алгоритми є безградієнтними методами, які працюють з популяцією кандидатних рішень, що підтримують різноманітність для створення нових рішень.

У звичайному ансамблевому навчанні з бегінгом мішки створюються один раз, і їхній вміст (навчальні приклади) фіксується протягом усього процесу навчання. У нашій статті ми пропонуємо еволюційний ансамблевий бегінг, де використовуємо еволюційні алгоритми для зміни вмісту мішків з метою ітеративного покращення ансамблю шляхом забезпечення різноманітності в мішках. Результати показують, що наш метод еволюційного ансамблевого бегінгу перевершує звичайні ансамблеві методи (бегінг та випадкові ліси) для кількох еталонних наборів даних за певних обмежень. Ми виявили, що еволюційний бегінг може природно підтримувати різноманітний набір мішків без зниження точності продуктивності."""
doc.add_paragraph(abstract_text)

doc.add_page_break()

# ============================================================================
# 1. ВСТУП
# ============================================================================

doc.add_heading('1. Вступ', level=2)

intro_text = """Мотивовані теоремою "безкоштовного обіду", методи ансамблевого навчання використовують різні методи для агрегації передбачень кількох моделей машинного навчання. Це часто призводить до значного покращення продуктивності порівняно з окремими моделями. Бегінг і бустінг є найпопулярнішими ансамблевими методами, причому випадковий ліс і AdaBoost є найбільш відомими реалізаціями відповідно.

Бегінг — це видатний ансамблевий метод для навчання окремих навчальних алгоритмів на довільних вибіркових підмножинах оригінального навчального набору даних. Агрегація кількох моделей призводить до нижчої дисперсії моделі, тоді як її зміщення може залишатися незмінним згідно з розкладом "зміщення-дисперсія" помилки для моделей машинного навчання.

Проблема бегінгу полягає в тому, що зміщення окремого навчального алгоритму в основному таке саме, як зміщення комбінованої моделі, оскільки краща продуктивність є єдиним результатом зменшеної дисперсії. Якщо окремі навчальні алгоритми досягають високого зміщення (недостатнє навчання моделі), ці помилки переносяться в агреговані передбачення."""
doc.add_paragraph(intro_text)

# ============================================================================
# 3. МЕТОДОЛОГІЯ
# ============================================================================

doc.add_heading('3. Методологія', level=2)

doc.add_paragraph("""
Наш метод еволюційного ансамблевого бегінгу (далі — EvoBagging) використовує еволюційний алгоритм для покращення вибірок мішків, згенерованих бегінгом. У бегінгу бутстреп ітеративно вибирає набір даних із заміщенням, доки не буде досягнуто бажаного розміру, і зазвичай використовується для контролю стабільності результатів у статистичному навчанні. Вміст усіх мішків ініціалізується на початку і залишається незмінним.

EvoBagging змінює вміст усіх мішків, застосовуючи оператори кросовера та мутації, надаючи перевагу мішкам (і їхнім відповідним навченим індивідуальним навчальним алгоритмам) з вищою продуктивністю класифікації. Ми також вводимо генетичний розрив з селекцією, щоб допомогти підтримувати різноманітність між індивідуальними навчальними алгоритмами та видаляти мішки з поганою продуктивністю.
""")

# --------------------------------------------------------------------------
# 3.1 Алгоритм EvoBagging
# --------------------------------------------------------------------------

doc.add_heading('3.1 Алгоритм EvoBagging', level=3)

doc.add_paragraph("""
Канонічний метод ансамблевого бегінгу передбачає генерацію випадкових підмножин навчальних зразків за допомогою бутстрепу та агрегацію результатів від окремих моделей, навчених на цих підмножинах. Бегінг вимагає двох гіперпараметрів: розміру вибірки S та кількості мішків N.

У EvoBagging ми використовуємо дерева рішень як призначену модель машинного навчання для мішків через її здатність до навчання, яка вимагає низьких обчислювальних витрат порівняно з іншими методами, такими як штучні нейронні мережі. Отже, індивіди в популяції EvoBagging — це мішки, що містять екземпляри даних. На кожному поколінні алгоритм створює нову популяцію індивідів, використовуючи генетичні оператори, такі як кросовер і мутація; отже, дані в мішках змінюються з часом.
""")

# --------------------------------------------------------------------------
# 3.2 Функція пристосованості (Fitness function)
# --------------------------------------------------------------------------

doc.add_heading('3.2 Функція пристосованості', level=3)

doc.add_paragraph("Після підгонки мішка з попередньо визначеною моделлю машинного навчання, пристосованість мішка залежить від його розміру (φ) відповідно до формули:")

# Формула (1)
p = doc.add_paragraph()
p.alignment = WD_ALIGN_PARAGRAPH.CENTER
math2docx.add_math(p, r"fitness(bag) = \alpha \times \frac{K + \phi_b}{K}")
p_num = doc.add_paragraph()
p_num.alignment = WD_ALIGN_PARAGRAPH.RIGHT
p_num.add_run('(1)')

doc.add_paragraph("де α — продуктивність класифікації моделі, пов'язаної з мішком (b) в ансамблі, а K — визначений користувачем гіперпараметр для заохочення більших мішків.")

# --------------------------------------------------------------------------
# 3.4 Кросовер і мутація
# --------------------------------------------------------------------------

doc.add_heading('3.4 Кросовер і мутація', level=3)

doc.add_paragraph("""
Мета оператора кросовера — "урізноманітнити" вміст мішка так, щоб модель, підігнана на його даних, мала кращу продуктивність. Ми спрощуємо крок вибору C батьків для кросовера, використовуючи схему рангової селекції, де нащадки з найвищою пристосованістю вибираються для кросовера.

Щодо мутації, M мішків будуть вибрані випадковим чином із поточної популяції P'. Нехай B — один із вибраних мішків, а B^c — множина всіх навчальних зразків, яких немає в B. Мішок B буде мутовано шляхом заміни вибраних випадкових зразків, заданих розміром мутації (MS) з B, такою ж кількістю випадкових зразків з B^c.
""")

# --------------------------------------------------------------------------
# 4.5.6 Вплив правила голосування
# --------------------------------------------------------------------------

doc.add_heading('Вплив правила голосування', level=3)

doc.add_paragraph("Правило голосування для агрегації передбачень окремих навчальних алгоритмів:")

# Формула (3)
p = doc.add_paragraph()
p.alignment = WD_ALIGN_PARAGRAPH.CENTER
math2docx.add_math(p, r"\hat{y} = \begin{cases} 0, & \text{якщо } \sum_{l \in L} P_{neg} \cdot acc_l < \sum_{l \in L} P_{pos} \cdot acc_l \\ 1, & \text{в іншому випадку} \end{cases}")
p_num = doc.add_paragraph()
p_num.alignment = WD_ALIGN_PARAGRAPH.RIGHT
p_num.add_run('(2)')

doc.add_paragraph("де l — окремий навчальний алгоритм у множині L, а acc_l — точність навчання l.")

# ============================================================================
# 4. РЕЗУЛЬТАТИ
# ============================================================================

doc.add_heading('4. Результати', level=2)

# --------------------------------------------------------------------------
# 4.3 Попередні результати
# --------------------------------------------------------------------------

doc.add_heading('4.3 Попередні результати', level=3)

doc.add_paragraph("EvoBagging перевершує бегінг та випадковий ліс для задач 6-bit та 8-bit parity:")

# Таблиця результатів для n-bit parity
table = doc.add_table(rows=4, cols=4)
table.style = 'Table Grid'

headers = ['Метод', '6-bit (10 bags)', '6-bit (100 bags)', '8-bit (100 bags)']
for i, h in enumerate(headers):
    cell = table.rows[0].cells[i]
    cell.text = h
    cell.paragraphs[0].runs[0].bold = True

results = [
    ['Бегінг', '69.3%', '100%', '88.2%'],
    ['Випадковий ліс', '65.0%', '100%', '89.1%'],
    ['EvoBagging', '75.0%', '100%', '92.2%']
]

for i, row in enumerate(results, start=1):
    for j, val in enumerate(row):
        table.rows[i].cells[j].text = val

doc.add_paragraph()

# --------------------------------------------------------------------------
# 4.5.1 Зменшення зміщення
# --------------------------------------------------------------------------

doc.add_heading('4.5.1 Зменшення зміщення (Bias)', level=3)

doc.add_paragraph("EvoBagging постійно зменшує середнє зміщення окремих навчальних алгоритмів:")

# Таблиця зменшення зміщення
table2 = doc.add_table(rows=5, cols=4)
table2.style = 'Table Grid'

headers2 = ['Набір даних', 'Початкове зміщення', 'Кінцеве зміщення', 'Зменшення']
for i, h in enumerate(headers2):
    cell = table2.rows[0].cells[i]
    cell.text = h
    cell.paragraphs[0].runs[0].bold = True

results2 = [
    ['Red wine', '0.072', '0.056', '22.0%'],
    ['Mnist', '0.091', '0.061', '32.2%'],
    ['Tic-tac-toe', '0.051', '0.035', '31.7%'],
    ['Two-spiral', '0.456', '0.446', '2.2%']
]

for i, row in enumerate(results2, start=1):
    for j, val in enumerate(row):
        table2.rows[i].cells[j].text = val

doc.add_paragraph()

# --------------------------------------------------------------------------
# 4.5.5 Результати на незбалансованих наборах даних
# --------------------------------------------------------------------------

doc.add_heading('4.5.5 Результати на незбалансованих наборах даних', level=3)

doc.add_paragraph("Продуктивність EvoBagging на незбалансованих наборах даних (F1-оцінка):")

# Таблиця для незбалансованих даних
table3 = doc.add_table(rows=5, cols=5)
table3.style = 'Table Grid'

headers3 = ['Набір даних', 'Дисбаланс', 'Бегінг', 'Випадковий ліс', 'EvoBagging']
for i, h in enumerate(headers3):
    cell = table3.rows[0].cells[i]
    cell.text = h
    cell.paragraphs[0].runs[0].bold = True

results3 = [
    ['Red wine', '1:6.35', '67.41%', '66.35%', '69.99%'],
    ['Pima', '1:3.68', '61.40%', '58.92%', '64.52%'],
    ['Abalone', '1:10.32', '30.81%', '15.28%', '34.60%']
]

for i, row in enumerate(results3, start=1):
    for j, val in enumerate(row):
        table3.rows[i].cells[j].text = val

# ============================================================================
# ВИСНОВКИ
# ============================================================================

doc.add_heading('Висновки', level=2)

conclusions = """1. Представлено нову реалізацію еволюційного алгоритму для ансамблевого навчання — EvoBagging.

2. EvoBagging покращує зміщення (помилку) в окремих навчальних алгоритмах із мішків, отриманих за допомогою бутстрепу.

3. EvoBagging успішно перевершує бегінг та випадкові ліси як для задач бінарної, так і багатокласової класифікації.

4. EvoBagging підтримує хорошу продуктивність як на збалансованих, так і на незбалансованих наборах даних.

5. Компоненти EvoBagging (кросовер, мутація та селекція на основі генетичного розриву) демонструють свою ефективність, забезпечуючи покращену точність продуктивності.

6. EvoBagging підтримує різноманітний ансамбль окремих навчальних алгоритмів, що є основним фактором покращеної продуктивності порівняно зі звичайними ансамблевими методами."""
doc.add_paragraph(conclusions)

# ============================================================================
# ЗБЕРЕЖЕННЯ
# ============================================================================

output_file = 'Evolutionary_Bagging_UKR.docx'
doc.save(output_file)

print("=" * 70)
print(f"✅ Документ '{output_file}' успішно створено українською мовою!")
print("=" * 70)
print("\n📄 Документ містить:")
print("   • Повну назву статті та інформацію про авторів")
print("   • Анотацію (Abstract)")
print("   • Вступ (Introduction)")
print("   • Розділ 3: Методологія")
print("     - 3.1 Алгоритм EvoBagging")
print("     - 3.2 Функція пристосованості (формула 1)")
print("     - 3.4 Кросовер і мутація")
print("     - Правило голосування (формула 2)")
print("   • Розділ 4: Результати")
print("     - 4.3 Результати на n-bit parity")
print("     - 4.5.1 Зменшення зміщення (Bias)")
print("     - 4.5.5 Результати на незбалансованих даних")
print("   • Висновки")